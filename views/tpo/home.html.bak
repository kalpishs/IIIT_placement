{{extend 'mylayout.html'}}
<h1>Welcome TPO Home</h1>
<form method="post" action="{{=URL('tpo','selected')}}">
<div class="welcome123">
    <div class="CSSTableGenerator" >
                <table >
                    <tr>
                        <td>
                           Company Name
                        </td>
                        <td >
                            CSV Status
                        </td>
                          <td >
                            Posting Status
                        </td>
                        <td>
                           DISPLAY
                        </td>
                    </tr>
                 {{for r in string:}}
            {{if r.find(',')==0:}}
                    {{r=r[1:]}}
                    <tr>
                        <td >
                           {{temp=r.split(',')}}
                           {{=temp[0]}}
                        </td>
                        <td style="color:red">
                             Incomplete
                        </td>
                            {{if str(temp[2])=='True':}}
                            <td style="color:green">
                                Active
                            </td>
                               {{else:}}
                             <td style="color:red">
                                InActive
                            </td>
                            {{pass}}

                            <td>
                        {{if str(temp[-1])=='True':}}
                                <input type="checkbox" name="{{=temp[1]}}" value="{{=temp[1]}}" checked="checked">
                                {{else:}}
                            <input type="checkbox" name="{{=temp[1]}}" value="{{=temp[1]}}">
                        {{pass}}
                        </td>

                    </tr>
                      {{pass}}
                      {{pass}}
                                {{count=0
                    for r in rows:
                    if count!=0:
                    temp=int(r.find(',,'))
                    cname=r.split(',')
                   }}
                 <tr>
                        <td >
                           {{=cname[3]}}
                        </td>

                 {{if r[0]=="," or r[-1]=="," or int(temp)!=-1: }}
                     <td style="color:red">
                     Incomplete
                     </td>
                    {{else:}}
                        <td style="color:green">
                     Complete
                        </td>
                     {{pass}}
                             {{if str(cname[5])=='True':}}
                            <td style="color:green">
                                Active
                            </td>
                               {{else:}}
                             <td style="color:red">
                                InActive
                            </td>
                            {{pass}}
                            <td>
                           {{if str(cname[6])=='True':}}
                           <input type="checkbox" name="{{=cname[4]}}" value="{{=cname[4]}}" checked="checked">
                          {{else:}}
                          <input type="checkbox" name="{{=cname[4]}}" value="{{=cname[4]}}" unchecked>
                         {{pass}}
                        </td>
                    </tr>
                {{pass}}{{count=count+1}}
                    {{pass}}
                    <tr>
                            <td colspan="4"><input type="submit" value="Submit"></td>
                   </tr>
        </table>
    </div>
      </div>
</form>
     <div class="clear123"></div>
